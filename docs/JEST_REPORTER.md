# Jest Custom Reporter

Test KanteenのJestカスタムレポーターを使用すると、テストの実行時情報（実行時間、成功/失敗、エラー詳細）を取得できます。

## インストール

```bash
npm install @koji-koji/test-kanteen
```

## 基本的な使い方

### jest.config.jsでの設定

```javascript
module.exports = {
  reporters: [
    'default', // Jest標準レポーターを維持
    ['@koji-koji/test-kanteen/jest', {
      output: './test-kanteen-runtime',
      format: ['json', 'markdown'],
      verbose: false
    }]
  ]
};
```

### TypeScriptでの設定

```typescript
// jest.config.ts
import type { Config } from 'jest';

const config: Config = {
  reporters: [
    'default',
    ['@koji-koji/test-kanteen/jest', {
      output: './test-kanteen-runtime',
      format: ['json', 'markdown'],
      verbose: false
    }]
  ]
};

export default config;
```

## オプション

| オプション | 型 | デフォルト | 説明 |
|-----------|-----|-----------|------|
| `output` | `string` | `'./test-kanteen-runtime'` | 出力ディレクトリ |
| `format` | `string[]` | `['json', 'markdown']` | 出力フォーマット (`json`, `markdown`) |
| `verbose` | `boolean` | `false` | 詳細ログを表示 |

## 出力フォーマット

### JSON形式 (`runtime-catalog.json`)

```json
{
  "metadata": {
    "version": "0.5.0",
    "generatedAt": "2025-11-15T10:30:00.000Z",
    "executionDate": "2025-11-15T10:29:55.000Z",
    "totalDuration": 5234,
    "sourceFiles": ["tests/math.test.ts"],
    "framework": "jest",
    "toolVersion": "0.5.0"
  },
  "testSuites": [
    {
      "id": "suite-1",
      "name": "Math",
      "filePath": "tests/math.test.ts",
      "runtime": {
        "duration": 523
      },
      "tests": [
        {
          "id": "test-1",
          "name": "should add two numbers",
          "location": {
            "file": "tests/math.test.ts",
            "line": 10,
            "column": 3
          },
          "runtime": {
            "duration": 45,
            "status": "passed"
          }
        },
        {
          "id": "test-2",
          "name": "should handle errors",
          "location": {
            "file": "tests/math.test.ts",
            "line": 15,
            "column": 3
          },
          "runtime": {
            "duration": 12,
            "status": "failed",
            "error": {
              "message": "Expected 2 to equal 3",
              "stack": "Error: Expected 2 to equal 3\n    at Object.<anonymous> (/tests/math.test.ts:16:5)",
              "matcherName": "toBe",
              "expected": "3",
              "actual": "2"
            }
          }
        }
      ]
    }
  ],
  "executionSummary": {
    "totalTests": 2,
    "passed": 1,
    "failed": 1,
    "skipped": 0,
    "pending": 0,
    "todo": 0,
    "totalDuration": 57,
    "startTime": "2025-11-15T10:29:55.000Z",
    "endTime": "2025-11-15T10:29:55.057Z"
  }
}
```

### Markdown形式 (`runtime-catalog.md`)

````markdown
# Runtime Test Catalog (Jest)

> Generated by Test Kanteen v0.5.0

## Metadata

- **Generated At**: 11/15/2025, 10:30:00 AM
- **Execution Date**: 11/15/2025, 10:29:55 AM
- **Total Duration**: 5234ms
- **Source Files**: 1

## Execution Summary

- **Total Tests**: 2
- **Passed**: 1 ✅
- **Failed**: 1 ❌
- **Skipped**: 0 ⏭️
- **Pending**: 0 ⏸️
- **Todo**: 0 📝

## Test Results

**Math** (523ms)
  - ✅ should add two numbers (45ms)
  - ❌ should handle errors (12ms)
    - ❌ Error: Expected 2 to equal 3
````

## 実行例

### 1. テストを実行

```bash
npm test
```

### 2. 出力を確認

```bash
ls test-kanteen-runtime/
# runtime-catalog.json
# runtime-catalog.md
```

### 3. JSON形式で確認

```bash
cat test-kanteen-runtime/runtime-catalog.json | jq '.executionSummary'
```

## ASTカタログとの比較

Test Kanteenは、ASTベースのカタログとランタイムカタログの両方を生成できます:

```bash
# 1. ASTカタログを生成
npx kanteen analyze "tests/**/*.test.ts"

# 2. ランタイムカタログを生成（Jestで実行）
npm test

# 3. 比較（Phase 5で実装予定）
npx kanteen compare \
  ./aaa_test_kanteen/catalog.json \
  ./test-kanteen-runtime/runtime-catalog.json
```

### ASTとランタイムの違い

| 項目 | ASTカタログ | ランタイムカタログ |
|------|------------|-----------------|
| 取得方法 | ソースコード解析 | テスト実行 |
| 実行時間 | ❌ | ✅ |
| 成功/失敗 | ❌ | ✅ |
| エラー詳細 | ❌ | ✅ |
| アサーション | ✅ | ❌ |
| 動的生成テスト | ❌ | ✅ (test.each等) |

## 高度な使い方

### 詳細ログの有効化

```javascript
module.exports = {
  reporters: [
    'default',
    ['@koji-koji/test-kanteen/jest', {
      output: './test-kanteen-runtime',
      format: ['json', 'markdown'],
      verbose: true  // 詳細ログを有効化
    }]
  ]
};
```

実行時の出力:
```
[Kanteen] Jest Reporter initialized
[Kanteen] Output: ./test-kanteen-runtime
[Kanteen] Format: json, markdown
[Kanteen] Test run started
[Kanteen] Processed 5 tests from /path/to/math.test.ts
[Kanteen] Processed 3 tests from /path/to/string.test.ts
[Kanteen] Test run completed
[Kanteen] Total tests: 8
[Kanteen] Passed: 7
[Kanteen] Failed: 1
[Kanteen] Duration: 150ms
[Kanteen] JSON catalog written to: ./test-kanteen-runtime/runtime-catalog.json
[Kanteen] Markdown catalog written to: ./test-kanteen-runtime/runtime-catalog.md
```

### JSON形式のみで出力

```javascript
module.exports = {
  reporters: [
    'default',
    ['@koji-koji/test-kanteen/jest', {
      output: './test-kanteen-runtime',
      format: ['json']  // Markdownは生成しない
    }]
  ]
};
```

## トラブルシューティング

### エラー: Cannot find module '@jest/reporters'

**原因**: `@jest/reporters`がインストールされていない

**解決策**:
```bash
npm install --save-dev @jest/reporters
```

### 出力ファイルが生成されない

**確認事項**:
1. テストが実際に実行されているか確認
2. `output`パスが正しいか確認
3. `verbose: true`で詳細ログを確認

### TypeScript型エラー

**解決策**: 型定義を確認
```typescript
import type { Config } from 'jest';

const config: Config = {
  reporters: [
    'default',
    ['@koji-koji/test-kanteen/jest' as any, {
      // ...options
    }]
  ]
};
```

## CI/CD統合

### GitHub Actions

```yaml
name: Test with Kanteen

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - run: npm ci
      - run: npm test

      - name: Upload test catalog
        uses: actions/upload-artifact@v4
        with:
          name: test-kanteen-runtime
          path: test-kanteen-runtime/
```

### GitLab CI

```yaml
test:
  script:
    - npm ci
    - npm test
  artifacts:
    paths:
      - test-kanteen-runtime/
    expire_in: 1 week
```

## 制限事項

- Jest 29.0.0以降が必要
- Node.js 18.0.0以降が必要
- 並列実行時のワーカー情報は現在未対応

## 関連ドキュメント

- [README.md](../README.md) - 基本的な使い方
- [LLM活用ガイド](./LLM_GUIDE.md) - LLMを使った高度な分析
- [CHANGELOG.md](../CHANGELOG.md) - 変更履歴

## サポート

問題が発生した場合:
- [GitHub Issues](https://github.com/koji-koji/test-kanteen/issues)
- [Discussion](https://github.com/koji-koji/test-kanteen/discussions)
